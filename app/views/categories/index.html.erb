<h1 class="text-2xl font-bold mb-4">Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.category_name, category_path(category), class: "text-blue-500 hover:underline" %></li>
  <% end %>
</ul>
